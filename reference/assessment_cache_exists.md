# Check if assessment cache exists

Check if assessment cache exists

## Usage

``` r
assessment_cache_exists(end_year, cache_key, max_age = 30)
```

## Arguments

- end_year:

  School year end

- cache_key:

  Cache type key

- max_age:

  Maximum age in days (default 30)

## Value

TRUE if valid cache exists
