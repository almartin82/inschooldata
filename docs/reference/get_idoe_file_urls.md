# Get URLs for IDOE enrollment data files

Returns a list of URLs for the various enrollment data files. IDOE uses
Excel files that contain multiple years of data.

## Usage

``` r
get_idoe_file_urls()
```

## Value

Named list of file URLs
