# Process raw directory data into standard format

Transforms raw IDOE directory data into a standardized schema with
consistent column names.

## Usage

``` r
process_directory(raw_data)
```

## Arguments

- raw_data:

  List containing corporation, school, and non_public_school data frames

## Value

Processed data frame with standardized columns
