# Check if raw cache is stale

Check if raw cache is stale

## Usage

``` r
is_raw_cache_stale(cache_path, max_age = 7)
```

## Arguments

- cache_path:

  Path to cache file

- max_age:

  Maximum age in days (default 7)

## Value

TRUE if cache is stale
